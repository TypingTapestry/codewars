let anarchy = function() {
    let arr = ['', 'a', 'b', 'c'];
    let one = arr.indexOf('a');
    let two = arr.indexOf('b');
    let three = arr.indexOf('c');
    return 'I can write numbers like, ' + one + ', ' + two + ', ' + three + '.';
}